[package]
name = "zstd_rs_wrapper"
version = "0.1.0"
description = "Rust wrapper around zstd compression library with C ABI"

[package.build]
backend = { name = "pixi-build-cmake", version = "*" }

[package.host-dependencies]
cmake = ">=3.20"
rust = ">=1.75"
